Generate publication-quality matplotlib figures from `ExperimentResults`, `ExperimentAnalysis`, `MethodComparison`, or `TrendAnalysis` and produce a `FigureSet`.

This skill uses `ctx.tools.llm_chat()` and `ctx.tools.execute_code()`.
