services:
  research-agent:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./configs/agent.yaml:/app/configs/agent.yaml
    restart: unless-stopped
